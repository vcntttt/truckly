{
  "name": "truckly-hono-api",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:push": "drizzle-kit push",
    "db:seed": "bun tsx src/db/seed.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1",
    "dotenv": "^16.5.0",
    "drizzle-orm": "^0.44.2",
    "hono": "^4.7.11"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.1",
    "tsx": "^4.20.3",
    "wrangler": "^4.4.0"
  }
}
